import { PipeOperationPipe } from './pipe-operation.pipe';

describe('PipeOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
